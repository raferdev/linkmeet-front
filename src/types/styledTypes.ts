export type UserHelloTypes = {
  position: string;
  height: string;
};

export type LoginBackgroundTypes = {
  display: string;
};

export type LoginFormTypes = {
  display: string;
};

export type MenuDivType = {
  display: string;
};
